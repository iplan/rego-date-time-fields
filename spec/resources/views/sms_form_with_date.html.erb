<%= form_for(sms, :url => 'http://google.com') do |f| %>
  <%= f.text_field :name %>
  <%
     options = {}
     options[:format] = date_format if local_assigns[:date_format].present?
  %>
  <%= f.date_field :sent_at_date, options %>
<% end %>
